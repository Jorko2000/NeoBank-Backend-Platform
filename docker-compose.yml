version: '3.8'
services:
  db:
    image: postgres
    environment:
      POSTGRES_DB: neobank
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"

  kafka:
    image: bitnami/kafka
    ports:
      - "9092:9092"
